{
  "name": "claude-code-agents-docs",
  "version": "1.0.0",
  "description": "Documentation deployment scripts for Claude Code Agents",
  "scripts": {
    "deploy:cloudflare": "mkdocs build && wrangler pages deploy site --project-name=claude-code-agents-docs",
    "deploy:preview": "mkdocs build && wrangler pages deploy site --project-name=claude-code-agents-docs --branch=preview"
  },
  "keywords": [
    "documentation",
    "mkdocs",
    "cloudflare-pages"
  ],
  "author": "Ronald DC",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
